type Seat {
  id: Int!
  status: String!
}
  
type Query {
  getSeat(id: Int!): Seat!
  allSeats: [Seat]
}
  
type Mutation {
  createSeat(status: String!): Seat!
  updateSeat(id: Int!, status: String!): Seat!
}

type Subscription {
  seatChanged: Seat!
  seatAdded: Seat!
}
